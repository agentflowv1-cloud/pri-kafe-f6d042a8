import React, { useState } from 'react'; import MenuItemForm from '../components/MenuItemForm'; import MenuItemsList from '../components/MenuItemsList'; const HomePage = () => { const [items, setItems] = useState<MenuItem[]>([]); const handleAddItem = (item: MenuItem) => { setItems((prevItems) => [...prevItems, item]); }; return ( <div className="home-page"> <h1>Pri Kafe Menu Management System</h1> <MenuItemForm onSubmit={handleAddItem} /> <MenuItemsList items={items} /> </div> ); }; export default HomePage;