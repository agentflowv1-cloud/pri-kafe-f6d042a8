import React, { useState } from 'react'; import { MenuItem } from '../types/MenuItem'; interface Props { onSubmit: (item: MenuItem) => void; } const MenuItemForm: React.FC<Props> = ({ onSubmit }) => { const [name, setName] = useState(''); const [price, setPrice] = useState(0); const [description, setDescription] = useState(''); const handleSubmit = (e: React.FormEvent<HTMLFormElement>) => { e.preventDefault(); const item: MenuItem = { id: Math.floor(Math.random() * 1000), name, price, description }; onSubmit(item); }; return ( <form onSubmit={handleSubmit}> <label>Name:</label> <input type="text" value={name} onChange={(e) => setName(e.target.value)} /> <br /> <label>Price:</label> <input type="number" value={price} onChange={(e) => setPrice(Number(e.target.value))} /> <br /> <label>Description:</label> <textarea value={description} onChange={(e) => setDescription(e.target.value)} /> <br /> <button type="submit">Add Item</button> </form> ); }; export default MenuItemForm;